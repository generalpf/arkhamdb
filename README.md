# arkhamdb

Arkham Horror 2nd Edition Database API

# Installation

```
$ make db
$ python3 -m venv env
$ source env/bin/activate
(env) $ pip install -r requirements.txt
```

# Running

```
(env) $ cd api
(env) $ flask run
```
